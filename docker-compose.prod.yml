services:
  db:
    image: postgres:16-alpine
    container_name: smartcontact_db_prod
    restart: always
    environment:
      POSTGRES_USER: ${DB_USER:-postgres}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-password}
      POSTGRES_DB: ${DB_NAME:-smartcontact}
    volumes:
      - smartcontact_prod_pgdata:/var/lib/postgresql/data
    networks:
      - smartcontact-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-postgres} -d ${DB_NAME:-smartcontact}"]
      interval: 10s
      timeout: 5s
      retries: 5

  api:
    build:
      context: ./backend
      dockerfile: Dockerfile.prod
    container_name: smartcontact_api_prod
    restart: always
    depends_on:
      db:
        condition: service_healthy
    environment:
      DB_TYPE: postgres
      DB_HOST: db
      DB_PORT: 5432
      DB_USERNAME: ${DB_USER:-postgres}
      DB_PASSWORD: ${DB_PASSWORD:-password}
      DB_DATABASE: ${DB_NAME:-smartcontact}
      JWT_SECRET: ${JWT_SECRET}
      NODE_ENV: production
      PORT: 3000
    # NÃO mapeamos porta 3000 no host para evitar conflito com mas-ia
    networks:
      - smartcontact-network

  web:
    build:
      context: ./frontend
      dockerfile: Dockerfile.prod
    container_name: smartcontact_web_prod
    restart: always
    ports:
      - "8081:80"
    depends_on:
      - api
    networks:
      - smartcontact-network

networks:
  smartcontact-network:
    driver: bridge

volumes:
  smartcontact_prod_pgdata:
